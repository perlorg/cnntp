
general: 
  display group.name as mail style name (perl5-porters vs perl.perl5.porters)

group list:
  - Add description to group list, then more data (Avg messages, last post ...)
  - nicer formatting

article list (in a group):
  - rss and atom feeds
  - show all messages, "not threaded"
  - show threads by first message in addition to by last message 

article view:
  - show all messages in the thread on one page (or a few pages... hmn)
    - flat? nested?

  - "next/previous thread" links


